name: Test bravis and sreality scrapers
on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install chromium
      - name: Run bravis scraper
        run: python bravis.py
      - name: Run sreality scraper
        run: python sreality.py
      - name: Commit and push JSON files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add bravis.json sreality.json
          git commit -m "Update bravis.json and sreality.json from GitHub Actions" || exit 0
          git push
